"""Ashta Chamma 3D — FastAPI backend application package.

This package is the root of the server-side codebase. Concrete modules
(routers, services, repositories, providers) will be added by subsequent
work orders. Keeping this file deliberately empty keeps the scaffold lean
and lets Ruff/Mypy run on a clean package from day one.
"""

__version__ = "0.1.0"
