// Vitest setup file — runs before each test suite.
// Extend vitest's expect with @testing-library/jest-dom matchers if needed.
// Currently no global setup required; add here as the test suite grows.
